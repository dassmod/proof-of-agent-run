# Worklog

One dated line per work session, newest on top: where the code stopped, the single next move, any open confusion. This exists because commits record what finished, not what was mid-thought; the next session reads the top line and starts moving in seconds. Purely technical, about the code's state, nothing else.

Format: `YYYY-MM-DD: stopped at X. Next: Y. Open: Z.`

---

- 2026-08-23: `TRACE-FORMAT.md` written at the repo root, and the engine caught up to one of its clauses. The spec states clause 1 (`inputs` keys are the guard's parameter names, executable chosen over stable, so a guard's parameter names are public interface and renaming one is a breaking change to the format) and clause 3 (three outcomes, could-not-check is a category rather than a status, and the honest sentence is "I could not follow step 4" and never "step 4 does not reproduce"). Clauses 2 and 5 are written as open rather than omitted, so the gap is visible in the document instead of only in this file. `replay_step` now returns a fourth status, `signature_mismatch`, when the recorded `inputs` keys do not fit the guard's parameters. The check is `inspect.signature(guard).bind(**step["inputs"])` inside a narrow `try`, not `try/except TypeError` around the guard call: `bind` performs the binding and stops, so a `TypeError` raised inside a guard's own body still escapes as the bug it is. The name accuses nobody on purpose, because three causes produce one signal (forged trace, refactored guard, honest rename drifting the two documents apart) and the engine cannot arbitrate between them. Before this, an unrunnable step raised out of `replay_trace` and erased the verdicts for every step the engine could have checked. `RENAMED_INPUT_RUN` (run-0004) added and wired into `check.py`: `target` in place of `path`, recording `"allow"` for `/etc/passwd`. The recorded decision is never read, and it is a lie on purpose, so the fixture demonstrates the trust boundary rather than merely exercising the branch: a forgery sitting under a `signature_mismatch` verdict, proving that could-not-check is not a pass. Output is now 0001 match, 0002 divergence, 0003 match, 0004 signature_mismatch with no `recorded=` tail. `check.py` needed no change to print the new status, because its detail block asks `if "recorded" in verdict` rather than indexing. Next: clause 3 owes one more sentence, that the narrowness of a `try` block is a design decision about who gets blamed, since the `**` unpack of `step["inputs"]` also sits inside the block and a trace whose `inputs` is not a mapping is indistinguishable at the `except` from an engine typo. Then clause 2 (bind `kind` to membership in `GUARDS`, or report coverage) and clause 5 (canonicalization for comparison; `==` is still the strictest possible policy arrived at by nobody choosing it). Open: still no fixture for the sibling-prefix case, so nothing here would catch `ALLOWED_PREFIXES` losing a trailing slash. Three stale-prose items found and two fixed: `runs.py`'s docstring said "Three runs" while a fourth was being added, and `engine.py` line 63 carried trailing whitespace on a blank line. The other two were in this file: `check.py` has no trailing whitespace and has not for some time, and `fixtures/` does have an `__init__.py`, so both of those open items were describing a state that no longer existed.

- 2026-08-17: no code written. The hour went into the material under the format, and one spec clause got settled: `inputs` keys are the guard's parameter names because `replay_step` calls `guard(**step["inputs"])`, so the trace is welded to the function signature. The cost of that binding is the spec's first real trade: rename `path` to `target` in six months and every trace ever recorded stops replaying, worldwide, permanently. Readable labels would survive any refactor and would not be callable. The format chose executable over stable, and the spec has to say so out loud rather than let the choice sit in a comment. Two behaviours confirmed by running them rather than by reading: a renamed or extra key in `inputs` raises `TypeError` at the unpack, so it never reaches a verdict at all, and a guard absent from `GUARDS` returns `unknown_guard`, which is the engine's own gap and never a claim about the run. Neither is `deny`, and the pull toward `deny` was the hour's main finding. A rewrite of `check_write_path` from memory (not cold, the file had been read first, so this measures copy drift and not recall) diverged in four places, ranked by loudness. `return deny` unquoted is a `NameError`, loud, dies pointing at itself. `path: Path` is inert at runtime and wrong underneath, since `pathlib.Path` instantiates as `PosixPath` or `WindowsPath` by host and carries `.resolve()` and `.exists()`, so the annotation re-invites OS-flavored, filesystem-touching types into the one file whose docstring forbids both. `{"workspace", "tmp"}` as a set iterates in a different order on every process, because string hashing is randomized per interpreter run, which puts nondeterministic iteration inside the deterministic slice; harmless today only because any match returns immediately, and wrong the moment one prefix is more specific than another. And `"workspace"` without the trailing slash silently returns `allow` for `workspace_secrets/keys.txt` and `workspaceevil/passwd`, which is the traversal bug's mirror image: prefix matching answering a question about characters while the question being asked is about structure. The repo's own constant is correct and needs no fix. Next: unchanged, write the trace-format spec, now with clause one settled and clause five named (what counts as equal, since `==` is currently choosing the strictest possible policy by nobody choosing). Open: no fixture covers the sibling-prefix case, so nothing in the repo would catch the trailing slash going missing. Open: `check.py` still carries one uncommitted line of trailing whitespace.

- 2026-08-12 (second session): guard hardened and the third fixture added. `check_write_path` now canonicalizes with `normpath` before prefix matching, because prefix matching asks a question about the characters at the front of a string and the string has to be in one form first. Imported from `posixpath`, not `os.path`: `os.path` is an alias that resolves to `ntpath` on Windows, so one recorded path would canonicalize two ways on two machines, and the host operating system is global state, which the guard's own admission test forbids. `Path.resolve()` was rejected for the same reason, one layer worse: it reads the filesystem to follow symlinks. Canonicalizing rather than banning `..` keeps `workspace/notes/../summary.md` working, which a ban would have denied. `TRAVERSAL_RUN` (run-0003) added: a path that begins with an allowed prefix and lands outside the room. It was written recording `allow` so the pre-fix guard would agree with it, and run in that state once, to watch the validator certify an escape with every step matching and nothing lying. Then converted to record `deny`, which turns it from a demonstration into an alarm: match while the guard stays correct, divergence the day the normalization is removed. A fixture that demonstrates a broken guard cannot outlive the repair. `check.py` at the root runs all three; current output is 0001 match, 0002 divergence, 0003 match. One class named tonight and it is the prose half of the usual one: fixing the guard silently invalidated its own docstring and the fixture's comment, both of which went on describing a state that no longer existed, with nothing to raise. Both rewritten to say the reason rather than the change. Next: unchanged, the trace-format spec, now owing one more answer. `kind` is written by the party under suspicion, so an agent that marks every step nondeterministic replays perfectly clean and has said nothing at all. Two candidate answers are recorded in `fixtures/runs.py`'s docstring: bind `kind` to membership in `GUARDS` so the trace stops getting a vote, or report coverage so a fully skipped run cannot read as a clean one. Open: canonicalization for comparison is still unwritten; `==` in `replay_step` survives only while guards return short strings. Open: `replay/` has an `__init__.py` and `fixtures/` does not, which works by namespace packages and is an accident rather than a decision.

- 2026-08-12: repo created; rung 1's core runs. `replay/guards.py` holds `ALLOWED_PREFIXES` and the pure `check_write_path(path) -> "allow" | "deny"`. `fixtures/runs.py` holds `HONEST_RUN` and `TAMPERED_RUN`, two steps each, every step carrying `kind` so the format itself declares which steps are replayable. `replay/engine.py` holds the `GUARDS` name-to-callable whitelist, `replay_step` and `replay_trace`. Verified end to end: run-0001 gives skipped then match, run-0002 gives skipped then divergence with `recorded: allow` against `rederived: deny`. Three bugs found on the way and they rank by loudness, which was the session's spine: `step{"name"}` is a SyntaxError and never loads, `step["nam"]` is a KeyError and dies pointing at itself, and `"status": "skipped"` hardcoded in the final return never fails at all and would have made the validator report every honest and every forged run identically. Third instance this week of the same class, after `PROMPT_HEADING` and the false `Rating.Again`: the output describes the observer's state rather than the world's. Next: write the trace-format spec from what this code actually needed, per Field.md M2 item 1, which was deferred on purpose so the format is evidence rather than a guess. It must state at minimum that `inputs` keys are the guard's parameter names, that `kind` is declared per step and not inferred, and that three outcomes exist and `unknown_guard` is never reported as divergence. Open: `check_write_path` uses `startswith`, so `workspace/../../etc/passwd` passes; resolve the path before checking, and this makes a second tampering fixture worth having. Pushed: root commit `ed1f82c`, 6 files, on GitHub as `dassmod/proof-of-agent-run`, **private on purpose** until the spec and the honest trust statement exist, because a public repo of working code with no README saying what it does not claim is the exact misreading this project is built to prevent. Also open and inherited from smart-repetition-agent: comparison here got away with plain `==` on short strings, and the moment a guard returns a dict or a float the canonicalization question arrives, unanswered in both repos.
